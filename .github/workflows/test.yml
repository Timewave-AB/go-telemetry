name: Tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    name: vet + race
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - run: ./run-tests.sh
