name: Tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    name: vet + race
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - run: ./run-tests.sh
